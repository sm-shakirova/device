import React from 'react';
import { Slider, Tabs } from '../../blocks';

const MainPage: React.FC = () => {
  return (
    <main>
      <Slider />
      <Tabs />
    </main>
  )
};

export default MainPage;
